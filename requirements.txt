# Vercel Python runtime instala desde aquí (raíz del repo).
# Para dev local usa `cd backend && uv sync` (pyproject.toml es la fuente).
# Mantener sincronizado: `cd backend && uv export --no-hashes > ../requirements.txt`

# Core
fastapi==0.115.4
pydantic==2.9.2
pydantic-settings==2.6.0
python-dotenv==1.0.1

# DB
sqlalchemy[asyncio]==2.0.36
alembic==1.13.3
psycopg[binary]==3.2.3
asyncpg==0.30.0

# Auth (JWT decode de Supabase)
pyjwt[crypto]==2.9.0
cryptography==43.0.3

# Supabase client (para casos donde FastAPI necesite hablar con Supabase)
supabase==2.10.0

# External services
openai==1.54.3
twilio==9.3.7
resend==2.4.0
stripe==11.2.0

# Observability
sentry-sdk[fastapi]==2.18.0

# HTTP client
httpx==0.27.2

# Utilities
python-multipart==0.0.17
email-validator==2.2.0
jinja2==3.1.4
