# Dependencias mínimas para que Vercel arranque.
# Pyproject de backend (cuando exista) será la fuente de verdad para dev local.
# A medida que cada bounded context aterrice en backend/src/, se agregan deps aquí.

fastapi==0.115.4
pydantic==2.9.2
